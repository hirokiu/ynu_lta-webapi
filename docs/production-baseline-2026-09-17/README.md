# 本番稼働確認版の保全 — 2026-09-17

このコミットは現行本番を基準にした保全であり、リファクタリングやバージョンアップではない。本番・DB・コンテナーは変更していない。タグ `production-baseline-2026-09-17` を今後の比較基準とする。「最新」は調査時点の本番稼働版を指し、未検証のMac変更やサーバー作業ツリーを最新版として採用していない。

秘密鍵・Firebase Admin資格情報・DB・証明書は含めない。起動には既存の資格情報と環境設定が別途必要。Macの既存作業ツリーとサーバー作業ツリーは変更していない。

## 比較資料

`source-hashes.json` に各ファイルのSHA-256を記録。production列の欠落はその取得方法では観測できなかったことを意味し、実行時に不存在とは限らない。GitHubの更新前との差分は本コミットのdiffで確認できる。

## 採用元と重要差分

- 正本は稼働中 `lta-api:/usr/src/app`。イメージID `sha256:570ca75e1d6da5c52f004b903e54856c37a69700fcb553744606699a5b7a2700`。ただしコンテナー内変更があるためイメージ単独を正本としない。
- GitHub・サーバーの基底HEAD: d5dc171c625b48a4acc967ac44ae77919efe7eab。
- `surveyApi.service.ts`: 本番には2026-01-10更新のCSV処理が存在。日本語の基本列（回答者ID・回答時刻・回答設問数）、設問番号順の列、複数選択の展開列、欠損列の正規化。Macは旧JMESPath形式。ユーザーに確認し、本番形式の採用を承認済み。
- CSVの回答時刻はlastOpenedAt/updatedAtを元に9時間加算する実装をそのまま保全。正しい提出時刻の定義やタイムゾーン処理は後続課題。回答内容のデバッグログも現状のまま残っており、後続で見直す。
- `main.controller.ts`: 本番は単発配信を30秒後に予約。Macのynu版も同じ動作（コメント・空白差）。Macの元版とサーバー版はその変更が未反映。
- `surveyApi.constants.ts`: 管理者一覧・接続先の差がある。今回追加の権限変更をせず本番設定を採用。
- Dockerfile、Compose、package.json、api.ts、firebaseAdmin.service.ts、tsconfig.jsonはサーバー作業ツリーが本番より古い部分を含む。Macは多くが本番と一致するが、CSV等の差があるため丸ごと採用しない。
- 本番package-lock.jsonを追加保全。サーバー作業ツリーには存在しなかった。依存の意図しない更新を防ぐ基準として記録する。

## 検証・制限

本番ログインと一覧閲覧は確認済み。本番コンテナー内の `tsc --noEmit --incremental false` はMongoDB型定義不足（TS2688/TS7016）で失敗。実行中の安定性確認と新規ビルドの再現性は別であり、本コミットをクリーンビルド検証済みとは呼ばない。依存・型定義修正は保全後の別コミットで実施する。通知やDBへの書込みを避けるため検証目的のAPI起動はしない。
