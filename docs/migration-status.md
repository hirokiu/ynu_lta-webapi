# 移行の全体ステップと現在地

2026-09-18更新。ユーザー方針：旧メールドメインの完全除去・既存アカウント移行を延期し、Survey改修の共同研究者確認とサーバー移行を優先する。

1. 保全：完了。新サーバーにDBと全56Survey出力を保管。Mac保存なし。
2. 新環境準備：完了。proto=既存Firebase+独立DBコピー、dev=kirokun-dev+空DB。dev Google管理者ログインを上松確認済み。
3. 既存認証でproto確認用公開：完了。https://proto.kirokun.alchembright.com/login。API 85b722d / Web e6199a5。UID制限で既存hanzawa/hasegawa/hiroki_uのみ許可。Firebaseのメール・パスワード・UIDは変更なし。API上のUID対応のみ新環境で適用。
4. 共同研究者確認・アカウント方針決定：現在地。Surveyページ・テンプレート・回答出力を確認。旧ドメイン除去と既存利用者の認証変更は保留。
5. 最終移行：未実施。旧正本の書込/定期処理停止、最終バックアップ、protoへ復元、旧アプリAPI接続維持、通知/回答準備と実機・復元・定期運用検証。
6. 旧サーバー解約：未実施。依存棚卸しと観察期間後。

## 確認用公開の状態

- DNSは新サーバー133.242.138.54。HTTPSログイン200、未認証の主要データAPI401、HTTP→HTTPS308を外部から確認。
- Caddy gatewayは~/kirokun-proto/ops/edge配下のComposeで稼働。公開ルートはproto→localhost8081のみ。devはSSH経由localhost18082を維持。
- 未登録UIDは旧管理者メールを名乗っても拒否。許可UIDは新サーバーのGit対象外.envに保持し、Git/文書へ載せない。
- 通知・配信準備false。既存アプリは旧環境のまま。画面に確認コピー/アプリ非連携/変更非同期/テンプレート保存の注意を表示。
- 確認中の変更は旧環境へ反映されない。最終移行時の扱いを整理し、残す設問はJSONで保存する。回答/配信コピーは本番へ逆同期しない。
- 更新前バックアップsurvey-20260918T044947Z-490134.archive.gz取得済み。Web/API/DBすべてhealthy。
- 共同研究者本人による新URLでのログイン・業務操作確認は未実施。案内送信は行っていない。
- 旧非公開stagingはメモリ確保のため停止、データ保持。旧本番には変更なし。

## 確認してもらう項目

従来ID/パスワードでログイン、Survey一覧10/20/50/100件、既存Surveyの設問テンプレート保存→新規読込→編集→保存、回答の全件/期間/選択出力、生成中表示。アプリ通知・新しい回答の収集は今回の確認範囲に含めない。

## アプリ実装の並行作業（2026-09-18）

利用者が共同研究者への確認依頼を送信済み。認証方針の回答待ちと並行して、両アプリにproto固定接続用ビルドを追加した。iOS Kirokun-Proto（ProtoDebug/ProtoRelease）、Android proto flavor。従来ビルドはFirebase Realtime Databaseのurl/stagingUrlを使用し続ける。共有Firebaseの接続先は変更しない。

iOSシミュレータービルド成功・生成plist確認、Android proto/legacyビルドとproto単体テスト成功。両GitHubリポジトリのrefactor/environment-routingブランチに記録。元のMacリポジトリは変更せず、特にiOSの既存未コミットscheme変更を保持した。

実機ログイン/回答送信/通知/配布は未実施。protoサーバーは現時点で既存3名限定・配信準備/通知停止。一般回答者へ配布する前に全既存利用者のUID対応と端末登録・回答処理・通知を検証する。上松専用dev版の別アプリID/Firebaseモバイル登録は後続。新ドメインだけでアプリ更新が必須という過去の説明を補足：現行ソースはFirebaseからURLを取得するため、実配布版の照合と互換性検証後なら共有URL切替による移行も検討可能。ただし今回はその値を変更していない。
